cmake_minimum_required(VERSION $ENV{ACC_CMAKE_VERSION})
project(bmad-lib)
set(LIBNAME bmad-lib)
SET (CREATE_SHARED true)
set (INC_DIRS .)
set (SRC_DIRS .)
include($ENV{ACC_BUILD_SYSTEM}/Master.cmake)
